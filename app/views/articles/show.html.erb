<h1><%= @article.title %></h1>

<p><%= @article.body %></p>


 <h4> <%= link_to "Edit", edit_article_path(@article) %></h4>

  <h4><%= link_to "Destroy", article_path(@article), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></h4>
 <h3><%= link_to "Back", articles_path(@articles) %></h3>
